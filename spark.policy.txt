grant {    permission java.security.AllPermission "*";};//grant {    // Allow reading all environment variables. //   permission java.lang.RuntimePermission "getenv.*";    // Allow controlling logging and all related logging permissions. //   permission java.util.logging.LoggingPermission "control";    // Allow all file operations (read, write, delete, and execute) on //all files.  //  permission java.io.FilePermission "<<ALL FILES>>", //"read,write,delete,execute";    // Allow all security permissions (this is very permissive).  //  permission java.security.SecurityPermission "*";    // Allow reflection on all classes.  //  permission java.lang.reflect.ReflectPermission "*";    // Allow access to all declared members.   // permission java.lang.RuntimePermission "accessDeclaredMembers";    // Allow dynamic code loading and definitions.   // permission java.lang.RuntimePermission "defineClassInPackage.*";//};